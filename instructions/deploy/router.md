# Deploy Router

Task type: `deploy`

Primary executor: `devops`.

Use this branch only when `current_stage = "issue_driven"` and the currently selected issue has `task_type = "deploy"`.

`deploy` is a post-setup task type, not a repository-wide stage in `.ai-dev-template.workflow-state.json`.

Read next:

1. `instructions/deploy/devops.md`

Do not read e2e instructions until rollout is complete.
