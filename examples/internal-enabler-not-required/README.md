# Internal Enabler Example

This package demonstrates:

- `internal_enabler` delivery unit;
- `architecture_decision_status = not_required`;
- repository and CI scaffolding work with no separate ADR.
